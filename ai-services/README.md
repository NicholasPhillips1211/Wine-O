# AI Services

This folder will hold microservices for detection (YOLOv8), OCR (EasyOCR), segmentation (SAM), embeddings (CLIP), and inference orchestration. Use GPU-enabled containers and model versioning.
